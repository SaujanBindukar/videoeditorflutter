name: video_editing_app
description: "A new Flutter project."

publish_to: "none"

version: 1.0.0+1

environment:
  sdk: ^3.10.3

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.8
  ffmpeg_kit_flutter_new: ^4.1.0
  file_picker: ^8.1.2
  path_provider: ^2.1.4
  video_trimmer: ^5.0.0
  gallery_saver: ^2.3.2
  video_player:
  path:

dev_dependencies:
  flutter_test:
    sdk: flutter

  flutter_lints: ^6.0.0

flutter:
  uses-material-design: true
  assets:
    - assets/fonts/Roboto.ttf
